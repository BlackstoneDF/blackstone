// test
{
    "playerAction": [
      {
        "blockType": "PlayerAction",
        "action": "SendMessage",
        "args": ["Text(s)"],
        // playerEvent. is auto appended at the start
        "rule": "sendMessage($)"
      },
    ] 
}